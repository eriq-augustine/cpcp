package com.cpcp.document;

/**
 * A general represenation of a classification document.
 * A document can be anything from a simple string to a complex structure.
 * It is the FeatureSetGenerator's job to extract features from a document.
 */
public interface Document {
   // Empty
}
